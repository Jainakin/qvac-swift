{
  "_comment": "Escape-hatch overrides for fields that an upstream exported contract incorrectly retains even though they are JavaScript-process callbacks rather than wire data.",

  "omitFields": {
    "_comment_omitFields": "Keys are <side>/<discriminator> (e.g. request/loadModel). Use * to apply to every leaf. The list is the property names to remove from the generated Codable struct.",
    "request/rag": ["onProgress"]
  }
}
